# TelegrammFinallyWorkOnSQL

📌 **TelegrammFinallyWorkOnSQL** — простой Telegram-бот, работающий с базой данных SQL.

Проект написан на Python и обеспечивает взаимодействие Telegram-бота с SQL-базой (SQLite / другая СУБД), позволяя сохранять, получать и обрабатывать данные через команды в чате.

## 🚀 Возможности

- Подключение к SQL-базе и выполнение операций CRUD
- Обработка команд Telegram
- Лёгкая архитектура с раздельными модулями
- Модельная структура для удобства расширения

## 📁 Структура проекта
├── main.py # Главный скрипт запуска бота

├── db.py # Подключение к базе данных

├── models.py # ORM/модели данных

├── words.db # Пример SQLite-БД

├── .gitignore

└── Screenshot…jpg # Скрин структуры базы данных

## Начало работы

- Скопируйте себе к себе в хранилище файлы репозитория
- Создай бота через @BotFather в Telegram и получи TOKEN
- Пропиши токен в main.py или через переменные окружения
- Запусти db.py а потом и сам бот main.py

## Установка библиотек и зависимостей

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Установка telebot и sqlAlchemy

```bash
pip install SQLAlchemy
pip install pyTelegramBotAPI
```




